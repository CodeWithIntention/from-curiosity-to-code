name: Build js-ide zip

on:
  push:
    paths:
      - '.github/workflows/js-ide-zip.yml'
      - 'docs/js-ide/**'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set release date
        run: echo "RELEASE_DATE=$(date -u +'%Y-%m-%d')" >> "$GITHUB_ENV"

      - name: Inject release date into js-release.js
        run: |
          sed -i "s/__RELEASE_DATE__/${RELEASE_DATE}/g" docs/js-ide/js-release.js

      - name: Build js-ide.zip
        run: |
          mkdir -p docs/downloads
          cd docs
          rm -f downloads/js-ide.zip
          zip -r downloads/js-ide.zip js-ide \
            -x "js-ide/.git/*" \
            -x "js-ide/**/.DS_Store"

      - name: Commit updated js-ide.zip
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add docs/downloads/js-ide.zip docs/js-ide/js-release.js

          if ! git diff --cached --quiet; then
            msg=$(git log -1 --pretty=%s --no-merges)
            if [ -z "$msg" ]; then
              msg=$(git log -1 --pretty=%s)
            fi
            git commit -m "zip: ${msg}"
            git push
          else
            echo "No changes to commit"
          fi
